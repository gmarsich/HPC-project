What to do before running the bash script:

THIN node:

salloc --exclusive -n 12 -N1 -p THIN --time=2:0:0
module load architecture/Intel
module load mkl
module load openBLAS/0.3.23-omp
export OMP_PLACES=cores
{
    export OMP_PROC_BIND=spread
        or
    export OMP_PROC_BIND=close
}
{
    If use BLIS:
    export BLIS_NUM_THREADS=12
    export LD_LIBRARY_PATH=/u/dssc/gmarsi00/myblis/lib:$LD_LIBRARY_PATH
}
srun -n1 make cpu





To make a .sh executable: chmod +x your_script.sh
To remove a file: rm file
To remove a folder: rm -rf
To change the name of a file: mv oldname newname
